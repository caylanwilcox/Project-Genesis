# ProfessionalChart Architecture Diagram

## 🏗️ Component Hierarchy

```
┌─────────────────────────────────────────────────────────────────┐
│                    RootChart.tsx (100 lines)                    │
│                  Root Orchestrator Component                     │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ useChartOrchestrator() - Master State Coordinator        │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
  ┌──────────┐         ┌─────────────┐      ┌──────────┐
  │ Header   │         │   Canvas    │      │  Footer  │
  └──────────┘         └─────────────┘      └──────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
  ┌──────────┐         ┌──────────┐         ┌──────────┐
  │MainChart │         │ Overlays │         │ Controls │
  │ (Canvas) │         │  Layer   │         │  Layer   │
  └──────────┘         └──────────┘         └──────────┘
```

## 📦 Module Structure (Elite Semantic Organization)

```
ProfessionalChart/
│
├── 🎯 RootChart.tsx (100 lines)
│   └─ Responsibility: Compose & orchestrate
│
├── 🧠 core/
│   └── state/
│       ├── useChartOrchestrator.ts (89)  → Master coordinator
│       ├── useTimeframeState.ts (77)     → Timeframe logic
│       └── useChartViewport.ts (45)      → Viewport state
│
├── 🎨 ui/
│   ├── layout/
│   │   ├── ChartCanvas.tsx (156)         → Main rendering area
│   │   ├── ChartInteractionHandlers.tsx  → Event wrappers
│   │   └── ChartFooter.tsx (34)          → Time display
│   └── overlays/
│       ├── PriceTagsOverlay.tsx (35)     → Y-axis labels
│       └── LoadingOverlay.tsx (24)       → Loading spinner
│
├── 👆 interactions/
│   ├── gestures/
│   │   ├── useHeightResize.ts (35)       → Drag height
│   │   └── useIntervalDropdown.ts (32)   → Click-outside
│   └── zoom/
│       └── ZoomControls.tsx (71)         → Zoom UI
│
└── 🛠️ utils/
    └── generators/
        └── mockDataGenerator.ts (29)     → Fallback data
```

## 🔄 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                     TickerPage (Parent)                     │
│  • Fetches data from Polygon API                            │
│  • Manages timeframe state                                  │
│  • Provides data to chart                                   │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ Props Flow
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                 ProfessionalChart (Root)                    │
│  • Receives: externalData, onTimeframeChange               │
│  • Orchestrates all sub-components                          │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              useChartOrchestrator Hook                      │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 1. useChartData → Processes external data            │  │
│  │ 2. useChartViewport → Manages pan/zoom offsets       │  │
│  │ 3. useChartScaling → Manages scale factors           │  │
│  │ 4. useTimeframeState → Manages timeframe selection   │  │
│  │ 5. useChartInteraction → Manages mouse/touch input   │  │
│  │ 6. useVisibleRange → Calculates visible candles      │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
           ┌───────────────┴───────────────┐
           │                               │
           ▼                               ▼
    ┌─────────────┐                ┌─────────────┐
    │ ChartCanvas │                │ChartHeader  │
    │  Renders:   │                │  Displays:  │
    │  • Candles  │                │  • Symbol   │
    │  • Volume   │                │  • Controls │
    │  • FVG      │                │  • Interval │
    │  • Overlays │                └─────────────┘
    └─────────────┘
```

## 🎮 User Interaction Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    User Actions                             │
└─────────────────────────────────────────────────────────────┘
    │         │           │            │            │
    │         │           │            │            │
    ▼         ▼           ▼            ▼            ▼
┌──────┐  ┌──────┐   ┌──────┐    ┌──────┐    ┌──────┐
│Click │  │Drag  │   │Scroll│    │Pinch │    │Click │
│1D/5D │  │Pan   │   │Zoom  │    │Zoom  │    │15min │
└──────┘  └──────┘   └──────┘    └──────┘    └──────┘
    │         │           │            │            │
    ▼         ▼           ▼            ▼            ▼
┌──────────────────────────────────────────────────────┐
│         useChartInteraction / useTimeframeState      │
└──────────────────────────────────────────────────────┘
    │         │           │            │            │
    ▼         ▼           ▼            ▼            ▼
┌──────┐  ┌──────┐   ┌──────┐    ┌──────┐    ┌──────┐
│Reset │  │Update│   │Update│    │Update│    │Fetch │
│Scales│  │Pan   │   │Scale │    │Scale │    │New   │
│      │  │Offset│   │      │    │      │    │Data  │
└──────┘  └──────┘   └──────┘    └──────┘    └──────┘
    │         │           │            │            │
    └─────────┴───────────┴────────────┴────────────┘
                        │
                        ▼
            ┌───────────────────────┐
            │    Chart Re-renders   │
            │  with Updated State   │
            └───────────────────────┘
```

## 🎨 Canvas Rendering Layers (Z-Index)

```
┌─────────────────────────────────────────────────────────┐
│                   ChartCanvas Container                 │
│  ┌────────────────────────────────────────────────────┐ │
│  │ Layer 0: Background Grid                          │ │
│  ├────────────────────────────────────────────────────┤ │
│  │ Layer 1: Market Hours Shading                     │ │
│  ├────────────────────────────────────────────────────┤ │
│  │ Layer 2: Fair Value Gaps (FVG)                    │ │
│  ├────────────────────────────────────────────────────┤ │
│  │ Layer 3: Candlesticks & Volume                    │ │
│  ├────────────────────────────────────────────────────┤ │
│  │ Layer 4: Price Lines (Entry/Stop/Targets)         │ │
│  ├────────────────────────────────────────────────────┤ │
│  │ Layer 5: Crosshair                                │ │
│  ├────────────────────────────────────────────────────┤ │
│  │ Layer 6: Price Tags Overlay (Y-Axis)              │ │
│  ├────────────────────────────────────────────────────┤ │
│  │ Layer 7: Zoom Controls (Drag Areas)               │ │
│  ├────────────────────────────────────────────────────┤ │
│  │ Layer 8: Loading Overlay                          │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## 🔀 State Management Architecture

```
┌─────────────────────────────────────────────────────────┐
│              useChartOrchestrator (Master)              │
└─────────────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│ Timeframe   │  │  Viewport   │  │   Scaling   │
│   State     │  │    State    │  │    State    │
├─────────────┤  ├─────────────┤  ├─────────────┤
│• display TF │  │• panOffset  │  │• priceScale │
│• data TF    │  │• priceOffset│  │• timeScale  │
│• interval   │  │• customH    │  │• handlers   │
│• dropdown   │  │• reset()    │  │• reset()    │
└─────────────┘  └─────────────┘  └─────────────┘
        │               │               │
        └───────────────┼───────────────┘
                        │
                        ▼
            ┌───────────────────────┐
            │   Derived State       │
            ├───────────────────────┤
            │• visibleRange         │
            │• chartData            │
            │• overlayTags          │
            └───────────────────────┘
```

## 📊 Timeframe Change Flow

```
User clicks "5D" button
        │
        ▼
handleTimeframeClick('5D')
        │
        ├─> resolveDisplayToData('5D')
        │   └─> { timeframe: '30m', intervalLabel: '30 min' }
        │
        ├─> setDisplayTimeframe('5D')
        ├─> setDataTimeframe('30m')
        ├─> setInterval('30 min')
        │
        ├─> onResetScales()
        │   ├─> setPanOffset(0)
        │   ├─> setPriceOffset(0)
        │   ├─> setPriceScale(1)
        │   └─> setTimeScale(1)
        │
        └─> onTimeframeChange('30m', '5D')
            │
            ▼
        Parent (TickerPage)
            │
            ├─> setTimeframe('30m')
            ├─> setDisplayTimeframe('5D')
            ├─> setAdditionalBarsToLoad(0)
            │
            └─> usePolygonData refetches with new params
                │
                ▼
        Chart receives new externalData
                │
                ▼
        useChartData processes data
                │
                ▼
        Chart re-renders with 30-min candles
```

## 🎯 Single Responsibility Principle Map

```
┌────────────────────────────────────────────────────────┐
│ Component                 │ Single Responsibility      │
├────────────────────────────────────────────────────────┤
│ RootChart                 │ Compose components         │
│ useChartOrchestrator      │ Coordinate state hooks     │
│ useTimeframeState         │ Manage timeframe logic     │
│ useChartViewport          │ Manage viewport offsets    │
│ useChartScaling           │ Manage zoom scales         │
│ useChartInteraction       │ Handle mouse/touch events  │
│ ChartCanvas               │ Compose rendering area     │
│ MainChart                 │ Render candlesticks        │
│ PriceTagsOverlay          │ Render Y-axis labels       │
│ LoadingOverlay            │ Show loading state         │
│ ZoomControls              │ Render zoom drag areas     │
│ ChartHeader               │ Render controls            │
│ ChartFooter               │ Render time display        │
└────────────────────────────────────────────────────────┘
```

## 🔍 Data Processing Pipeline

```
Polygon API
    │
    └─> Raw Aggregates (bars)
            │
            ▼
    usePolygonData Hook
            │
            ├─> Normalizes to CandleData[]
            ├─> Filters by timeframe
            └─> Sorts by timestamp
            │
            ▼
    ProfessionalChart (externalData prop)
            │
            ▼
    useChartData Hook
            │
            ├─> Validates data
            ├─> Fallback to mock if empty
            └─> Triggers onDataUpdate callback
            │
            ▼
    chartData (memoized)
            │
            ▼
    useVisibleRange Hook
            │
            ├─> Applies panOffset
            ├─> Applies timeScale
            ├─> Calculates visible window
            └─> Returns { start, end }
            │
            ▼
    MainChart Component
            │
            ├─> Slices data[start:end]
            ├─> Draws candles on canvas
            ├─> Draws volume bars
            ├─> Draws FVG patterns
            └─> Renders to screen
```

## 🚀 Performance Optimizations

```
┌────────────────────────────────────────────────────────┐
│ Optimization                │ Implementation           │
├────────────────────────────────────────────────────────┤
│ Memoized chartData          │ useMemo([data, price])   │
│ Memoized visibleRange       │ useVisibleRange hook     │
│ Callback stability          │ useCallback everywhere   │
│ Canvas rendering            │ Only visible candles     │
│ Event handler pooling       │ Shared mouse/touch logic │
│ Interaction debouncing      │ RAF for zoom/pan         │
│ Conditional rendering       │ Only render when visible │
│ Lazy component loading      │ dynamic() for chart      │
└────────────────────────────────────────────────────────┘
```

---

## 📝 Key Takeaways

1. **Orchestration Pattern**: `useChartOrchestrator` coordinates all state
2. **Semantic Organization**: Clear folder structure by responsibility
3. **Single Responsibility**: Each module does ONE thing well
4. **Type Safety**: Full TypeScript coverage, no `any` types
5. **Performance**: Memoization, callbacks, and efficient rendering
6. **Maintainability**: Easy to find and modify specific functionality

**Total Lines of New Semantic Structure**: ~779 lines
**Largest File**: ChartCanvas.tsx (156 lines)
**Average File Size**: ~65 lines
**Build Status**: ✅ Successful
